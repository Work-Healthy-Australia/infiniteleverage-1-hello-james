# Infinite Leverage — environment template
# Copy to .env.local and fill in real values. Never commit .env.local.

# Booking link (defaults to https://cal.com in code)
# NEXT_PUBLIC_CAL_URL=https://cal.com/your-username
